% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_data_sim.R
\name{sim_signalsum}
\alias{sim_signalsum}
\title{Generate a Sum of Cosine Signals}
\usage{
sim_signalsum(
  x,
  T,
  A = rep(1, length(freq)),
  phi = rep(0, length(freq)),
  tmin = rep(1, length(freq)),
  tmax = rep(length(x), length(freq))
)
}
\arguments{
\item{x}{Numeric vector. The time or space points at which to evaluate the signals.}

\item{T}{Numeric vector. Periods of the cosine signals.}

\item{A}{Numeric vector. Amplitudes of each cosine signal. Default is 1 for each period.}

\item{phi}{Numeric vector. Phase shifts for each cosine signal. Default is 0 for each period.}

\item{tmin}{Integer vector. Starting indices for each signal (values before are set to 0). Default is 1 for each signal.}

\item{tmax}{Integer vector. Ending indices for each signal (values after are set to 0). Default is \code{length(x)} for each signal.}
}
\value{
Numeric vector of the same length as \code{x} containing the summed signal.
}
\description{
This function generates a signal by summing multiple cosine signals with
specified periods, amplitudes, phase shifts, and optional truncations.
}
\examples{
x <- 1:100
y <- sim_signalsum(x, T = c(20, 50), A = c(1, 0.5))
plot(x, y, type = "l")

}
