% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_data_sim.R
\name{sim_signal}
\alias{sim_signal}
\title{Generate a Single Cosine Signal}
\usage{
sim_signal(x, T, A = 1, phi = 0, tmin = 1, tmax = length(x))
}
\arguments{
\item{x}{Numeric vector. The time or space points at which to evaluate the signal.}

\item{T}{Numeric. Period of the cosine signal.}

\item{A}{Numeric. Amplitude of the signal. Default is 1.}

\item{phi}{Numeric. Phase shift as a fraction of the period. Default is 0.}

\item{tmin}{Integer. Index from which the signal starts (values before are set to 0). Default is 1.}

\item{tmax}{Integer. Index at which the signal ends (values after are set to 0). Default is \code{length(x)}.}
}
\value{
Numeric vector of the same length as \code{x} containing the generated cosine signal.
}
\description{
This function generates a cosine signal of length \code{x} with specified period,
amplitude, phase shift, and optional truncation at the beginning and end.
}
\examples{
x <- 1:100
y <- sim_signal(x, T = 20, A = 2, phi = 0.25)
plot(x, y, type = "l")

}
